# ~/.tmuxinator/admin.yml
# you can make as many tabs as you wish...

project_name: admin
project_root: ~/
tabs:
  - shell:
  - troy: autossh -M 45863 troy -t dtach -A /tmp/$(whoami)-admin.dtach -r winch -E zsh 
 
